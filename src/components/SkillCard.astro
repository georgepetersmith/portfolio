---
interface Props {
    skill: string;
    icon: string;
}

const { skill, icon } = Astro.props as Props;
---

<figure>
    <img src={icon} alt={skill} width="50px" height="50px" />
    <figcaption>{skill}</figcaption>
</figure>
