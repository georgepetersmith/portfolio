---
interface Props {
    skill: string;
    src: string;
}

const { skill, src } = Astro.props as Props;
---

<figure>
    <img src={src} alt={skill} width="50px" height="50px" />
    <figcaption>{skill}</figcaption>
</figure>
